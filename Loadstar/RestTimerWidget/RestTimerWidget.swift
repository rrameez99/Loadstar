//
//  RestTimerWidget.swift
//  RestTimerWidget
//
//  Reserved for a Home Screen widget (recovery score at a glance).
//
//  Xcode's template put a placeholder widget here that displayed an emoji. It's
//  been emptied rather than deleted because a Home Screen widget is a genuine
//  future addition — it just needs App Groups first, so the extension can read
//  the app's SwiftData store. Until then the bundle ships only the Live Activity.
//

import WidgetKit
import SwiftUI
