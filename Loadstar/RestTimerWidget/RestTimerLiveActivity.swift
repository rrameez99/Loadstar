//
//  RestTimerLiveActivity.swift
//  RestTimerWidget
//
//  Empty on purpose.
//
//  This file was written before the widget target existed. Xcode then generated
//  RestTimerWidgetLiveActivity.swift for the same job, and the implementation now
//  lives there — keeping both would define two Live Activity widgets and the
//  bundle would only ever show one of them.
//
//  Safe to delete in Xcode whenever convenient (right-click → Delete → Move to
//  Trash). Left in place because the folder is file-system synchronized, so an
//  orphaned file with real content would still get compiled.
//
